/* Requires the Docker Pipeline plugin */

pipeline {
    agent { 'remote' }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
